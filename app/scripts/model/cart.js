import Backbone from 'backbone';

export default Backbone.Model.extend({
  // idAttribute: 'id',
  defaults: {
    // 'inCart': false,
  },
  addItem: function() {

  },
  removeItem: function() {

  },
});
